# Agent Pages — Product direction

Updated: 2026-09-05. Status: the single-owner MVP is implemented and locally verified; deployment through the intended proxy is deferred until domains and a target are chosen.

## Purpose

Agent Pages is a small, self-hosted application for coding agents and their owners to share simple static test sites. It is a developer tool, not a hosting service: hosted sites are excluded from search indexing. An owner creates a site through MCP or REST, receives a stable URL, inspects selected files and updates the same site with a small number of operations.

Every site starts **private to its owner**. The owner can explicitly make it public, and can return it to private visibility. Publication of new content and making a site public are separate operations.

## Authoritative documents

- [MVP specification](.scratch/mvp/spec.md): current behavior, transport contracts, limits and acceptance criteria.
- [Implementation plan](.scratch/mvp/plan.md): ordered tickets, readiness and remaining decisions.
- [Pre-release deltas](.scratch/pre-release/spec.md): decisions and tickets that amend the MVP contract before the first deployment.
- [Domain glossary](CONTEXT.md): canonical terms.
- [Origin isolation decision](docs/adr/0001-isolate-site-origins.md) and [publication decision](docs/adr/0002-publish-complete-revisions.md): durable trade-offs.
- [Original specification](docs/archive/agent-pages-spec-original.md): historical reference only, superseded by these documents.

The MVP specification owns executable requirements. This document owns product scope and sequencing; tickets point to the contract rather than redefining it. Proposed later features are not MVP requirements.

## Product principles

- Preserve a site's identity and URL across updates.
- Make privacy the default and exposure an explicit owner action.
- Return predictable results for retries, conflicts and failures.
- Treat uploaded HTML, JavaScript and assets as untrusted browser content.
- Keep installation to one application, one running instance and one persistent volume.
- Keep maintenance operations possible without a hosted control-plane dependency.
- Implement shared domain operations used by REST, MCP and the web interface.

## Deployment shape

```text
app.example.com                  → login, owner dashboard, REST, MCP
<site-id>.sites.example.com       → one site's pages and assets
                                 → same application/container and /data volume
```

Production requires wildcard DNS, TLS and reverse-proxy routing for the content hostnames. Site IDs are generated by the server. Uploaded sites run at `/` on their own hostname; relative and root-relative asset URLs work without rewriting. Management routes are not available on content hostnames.

This intentionally replaces the original one-domain and `/s/:slug/` model. The additional DNS setup is required by the owner-only privacy model while supporting untrusted client-side JavaScript.

## Initial MVP

The first MVP provisions only the user's own account; multiple-account support is deferred. Each site still records and enforces its owner explicitly.

The first useful release must let an owner:

1. Sign in and obtain a personal API key.
2. Create a private multipage static site using MCP or REST.
3. Open the site while signed in, with pages and assets protected alike.
4. List/read selected files, then apply a complete batch update at the same URL.
5. Make the site public explicitly and later make it private again.
6. Delete files or the site, choose the default expiration for new sites and change a live site's expiration.
7. Export a live site’s active revision as a ZIP from the dashboard or authenticated REST.
8. Repeat the workflow from **both Codex and Claude Code**.

The web interface provides ZIP export, login/logout, the owner's site list and detail, opening a site, expiration and visibility controls and personal API-key management. File upload is initially through REST/MCP; browser drag-and-drop, ZIP import and an editor are later work.

Integrity and access checks are part of the MVP: complete revision publication, optimistic concurrency, idempotency, resource limits, expiration enforcement, restart recovery and a tested backup/restore procedure.

## Stack

Use one TypeScript package with pnpm, Node.js, TanStack Start and React. Use SQLite with explicit parameterized queries for metadata ([ADR 0003](docs/adr/0003-explicit-sqlite-persistence.md)), local filesystem for content, Zod at external inputs, and the official MCP TypeScript SDK with Streamable HTTP. Use Tailwind and small accessible components for the owner interface.

Keep TanStack routing as the HTTP integration point. Add TanStack Query only where the web interface has a concrete need for its cache. Select mutually compatible stable dependency versions during the first ticket, then pin the runtime/package manager and generate a lockfile.

One domain module owns creation, publication, visibility, quotas and deletion. The content-serving module reads a selected revision; the identity module resolves authenticated owners. Avoid a generic provider framework, a second HTTP framework or multiple application packages until a demonstrated constraint requires them.

## After the MVP

| Stage | Additions | Reason to start |
| --- | --- | --- |
| Usability | Browser multi-file upload, ZIP import, SPA mode | Real workflows are blocked by the initial upload or routing model |
| Recovery | Previous-revision restore | Owners need to undo edits or move content independently |
| Distribution | Public Docker image, installation improvements, license selection before distribution | Fresh installations are repeatable and security checks pass |
| Storage and workflow | CLI, S3-compatible storage, snapshots | Measured operations or storage constraints justify them |

Do not build billing, teams, organizations, public registration, invitations, client approval workflows, analytics, Git deployments, server-side execution, SSR for uploads or per-site containers for the MVP. Private sharing with other people is also out of scope: a private site is owner-only, while a public site is visible to everyone.

## Success criteria

Evaluate the product using observed workflows rather than feature count:

- Both selected clients can create and update a real site without manually repairing requests.
- An owner can change one stylesheet without sending the entire site back through MCP.
- Unauthenticated visitors and other owners cannot read a private page, asset or management response.
- Failed updates preserve the previously active revision.
- Restart and restore preserve ownership, visibility, content and recoverable operation results.
- Installation instructions succeed against a fresh volume behind the intended proxy.

The implementation plan records evidence and remaining limitations; planning or scaffolding alone is not completion.
